// Define configuration object
const config = {
    details: {
        host: "http://localhost",
        port: "3001"
    },
    routes: {
        home: "/",
        login: "/login",
        register: "/register"
    }
}

// Export
export default config